// import { VElement, VDocumentFragment, vdom } from './vdom.ts'

// const doc = typeof document === 'undefined' ? vdom : document

// export function render(element: VElement, target: Element): VDocumentFragment {
//   if (typeof document === 'undefined') {

//   }
//   return Element
// }

// export function ssr(element: VElement): string {

// }
